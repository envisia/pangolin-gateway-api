{
  "http": {
    "routers": {
      "ip-router": {
        "entryPoints": ["web"],
        "rule": "Host(`ip.integration.local`)",
        "service": "ip-service",
        "priority": 100
      },
      "fqdn-router": {
        "entryPoints": ["web"],
        "rule": "Host(`fqdn.integration.local`)",
        "service": "fqdn-service",
        "priority": 100
      },
      "dns-router": {
        "entryPoints": ["web"],
        "rule": "Host(`dns.integration.local`)",
        "service": "dns-service",
        "priority": 100
      }
    },
    "services": {
      "ip-service": {
        "loadBalancer": {
          "servers": [
            { "url": "http://10.0.0.42:8080" }
          ]
        }
      },
      "fqdn-service": {
        "loadBalancer": {
          "servers": [
            { "url": "https://upstream.integration.local" }
          ]
        }
      },
      "dns-service": {
        "loadBalancer": {
          "servers": [
            { "url": "http://example-backend.default.svc.cluster.local:80" }
          ]
        }
      }
    }
  }
}
